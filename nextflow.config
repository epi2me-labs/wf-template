//process.container = 'staphb/nanoplot'
docker.runOptions = '-u $(id -u):$(id -g)'
docker.enabled= true

params {
    input= "subset_reads.fq" 
    outdir="subset_results"
    minlength = 1000 
    keep_percent = 90
    targetbases = 500000000

}
