{
  "schemaVersion": "1.0",
  "description": "Schema for NanoPlot Nextflow pipeline",
  "processes": [
    {
      "name": "NANOPLOT",
      "description": "Process to run NanoPlot for quality control of ONT reads",
      "params": {
        "input": {
          "type": "string",
          "description": "Path to the input ONT reads file (FASTQ format). Can include wildcards for multiple files.",
          "default": null
        },
        "outdir": {
          "type": "string",
          "description": "Directory where output files will be saved.",
          "default": "./results"
        }
      }
    }
  ],
  "workflow": {
    "params": {
      "input": {
        "type": "string",
        "description": "Path to the input ONT reads file (FASTQ format). Can include wildcards for multiple files.",
        "required": true
      },
      "outdir": {
        "type": "string",
        "description": "Directory where output files will be saved.",
        "default": "./results"
      }
    },
    "metadata": {
      "name": "NanoPlot Workflow",
      "description": "A Nextflow pipeline to run NanoPlot for quality control of ONT reads.",
      "author": "Nabil Asyraf",
      "contact": "nabilash619@gmail.com"
    }
  }
}

